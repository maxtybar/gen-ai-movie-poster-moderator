FROM python:3.11
COPY . /workdir
WORKDIR /workdir
RUN pip install -r requirements.txt

ARG GRADIO_USERNAME
ARG GRADIO_PASSWORD
ENV GRADIO_USERNAME=$GRADIO_USERNAME
ENV GRADIO_PASSWORD=$GRADIO_PASSWORD
ENV PYTHONUNBUFFERED=1

EXPOSE 80
ENTRYPOINT [ "python" , "app.py" ]